# Changes - not in documentation yet

- Candidate field type documented across project docs (`README`, `technical-documentation`, `conventions`):
  - new config type `candidate` with `candidate.targetField` and optional `candidate.inputType`
  - sidebar inline accept action and target-type behavior (including `wikidata-autosuggest` prefill + immediate search)
  - validation constraints for allowed targets and non-empty target references


--
